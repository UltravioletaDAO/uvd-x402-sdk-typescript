---
date: 2026-09-13
tags:
  - type/handoff
  - domain/x402
  - domain/sdk-typescript
  - priority/p1
status: active
---

# La ventana EIP-3009 es un parámetro, y su default es el que publica el facilitador

**Versión:** 2.91.0 · **Toca dinero:** decide cuánto tiempo vive una autorización
firmada, o sea si un `settle` legítimo llega a tiempo o expira. · **Issue #2.**

## Qué cambió

Hasta 2.90.0 la ventana de validez de la autorización EIP-3009
(`validBefore = now + N`) era **300 s en Base y 60 s en las otras once redes EVM**,
escrita dos veces y sin forma de sobreescribirla:

| dónde | antes (origin/main `b465534`) | ahora |
|---|---|---|
| `src/client/X402Client.ts:1074` | `chain.name === 'base' ? 300 : 60` | `resolveValiditySeconds(paymentInfo.validitySeconds, this.config.validitySeconds)` (`:1076`) |
| `src/providers/evm/index.ts:305` | `chainConfig.name === 'base' ? 300 : 60` | `resolveValiditySeconds(paymentInfo.validitySeconds)` (`:307`) |

Lo nuevo, en una sola definición que leen los dos caminos de firma:

- **`src/utils/validity.ts`** — `DEFAULT_VALIDITY_SECONDS = 300`,
  `MAX_VALIDITY_SECONDS = 3600` y
  `resolveValiditySeconds(perPayment?, clientDefault?)`.
- **`PaymentInfo.validitySeconds`** (`src/types/index.ts:302`) — la ventana de
  **ese** pago.
- **`X402ClientConfig.validitySeconds`** (`src/types/index.ts:902`) — el default
  **de ese cliente**.
- Exportados al público en `src/utils/index.ts:45-48` y `src/index.ts:249-251`,
  para que un consumidor **lea** el valor efectivo en vez de re-tipear `300` en su
  propia aritmética de timeouts.

**Precedencia:** el pago gana al cliente, el cliente gana al default.
Un valor que no sea un entero entre 1 y 3600 se rechaza con
`X402Error('INVALID_CONFIG')` **antes de firmar**, en vez de caer en silencio al
default: quien pidió una ventana y recibió otra estaría firmando algo que no
eligió, y el error solo aparecería mucho después como un `settle` fallido.

**Por qué hay techo** (lo puso la revisión de seguridad, ver más abajo): este
tipo tiene la forma de un 402 parseado —`PaymentInfo` está documentado como
«payment information returned by backend on 402 response»— y
`src/react/PaymentMethodPicker.tsx:79` hace `pay({ ...payment, accept })` con un
`payment` que arma el integrador. Quien le pase la respuesta del vendedor tal
cual le estaría dando **al vendedor** la decisión de cuánto vive la autorización
del pagador. Una ventana de un año es un derecho de cobro en pie: el comprador la
da por vencida y se liquida meses después, contra una billetera con saldo.
3600 no es un número redondo elegido a gusto: es el `valid_duration` por default
del SDK Python, o sea lo más largo que firma hoy cualquier cosa del stack, así
que el techo no rechaza nada que hoy funcione.

## Por qué 300 y no 60 ni 3600 (todo medido, nada a ojo)

1. **300 es el número del vendedor, no mío.** El facilitador publica
   `max_timeout_seconds: 300` en su propio discovery: `x402-rs` origin/main
   `src/discovery.rs:1318`, `:1711`, `:1745`.
2. **El facilitador solo rechaza ventanas CORTAS.** `assert_time` en `x402-rs`
   `src/chain/evm.rs:1607-1623` falla con `Expired` cuando
   `valid_before < now + 6 s` (`CLOCK_SKEW_GRACE_SECS = 6`, `:1612`), y **no tiene
   techo superior**. Ampliar la ventana no puede convertir en fallo un pago que
   hoy pasa; con 60 s el pagador tenía ~54 s reales para abrir la billetera y
   firmar.
3. **El daño es de producción, no teórico.** Un vendedor que liquida async
   —verificar, entregar el recurso, liquidar después— tiene que cerrar el `settle`
   dentro de la ventana o la autorización expira sola y revoca un acceso que **sí**
   se pagó. Es el flujo de MeshRelay Turnstile.
4. **Los adaptadores de este mismo SDK ya usaban 300**: `src/adapters/wagmi.ts:142`
   (`validitySeconds = 300`), `src/adapters/ows.ts:243`, `src/adapters/env-key.ts:161`.
   El nombre `validitySeconds` sale de ahí; no inventé uno nuevo.
5. **El SDK Python usa 3600** (`uvd-x402-sdk-python` `src/uvd_x402_sdk/client.py:1839`).
   No lo toqué: los dos SDK ya son configurables y el default de TypeScript es el
   que el facilitador anuncia. La diferencia queda como fila de backlog.

## Compatibilidad

- **Base no cambia**: 300 s antes y después.
- **Las otras once redes EVM pasan de 60 s a 300 s** sin que nadie configure nada.
  Es el cambio de comportamiento de esta versión y es deliberado.
- Quien no escriba `validitySeconds` en ningún lado no cambia una línea de código.
- El header emitido sigue byte-idéntico salvo por el `validBefore`.

## El test discriminante

`src/validity-window.test.ts` (nuevo, 14 casos). Se probó a mano que discrimina:

| estado del código | comando | resultado |
|---|---|---|
| dos sitios revertidos a `origin/main` (`git checkout -- src/client/X402Client.ts src/providers/evm/index.ts`) | `npx vitest run src/validity-window.test.ts` | **13 failed \| 1 passed (14)** — `AssertionError: expected 60 to be greater than or equal to 297`, y en los casos de rechazo `promise resolved "{ success: true, …(4) }" instead of rejecting` |
| con el cambio (`git apply` del mismo diff) | `npx vitest run src/validity-window.test.ts` | **14 passed (14)** |

El único caso que pasa en ROJO es *«keeps the 300 s Base already had»*, que es
exactamente lo correcto: Base ya tenía 300 y no debía moverse.

El caso *«the two signing paths read the same parameter»* recorre `base`,
`avalanche` y `polygon` y exige que `X402Client.createPayment()` y
`EVMProvider.signPayment()` firmen **la misma** ventana: es el test que se pone
rojo si alguien vuelve a editar un sitio y no el otro. Las ventanas se miden
contra el reloj real con 1 s de holgura (`expectWindow`), no con timers falsos,
porque `ethers` firma en milisegundos y un reloj congelado escondería la
aritmética que se quiere verificar.

## Pre-CI local (todo verde, en este worktree)

| paso | comando | resultado |
|---|---|---|
| install | `npm ci` | exit 0 |
| typecheck | `npm run typecheck` | exit 0 |
| tests | `npm run test:run` | exit 0 — **695 passed / 41 archivos**, 14 de ellos en el archivo nuevo |
| lint | `npm run lint` | exit 0, sin warnings |
| build | `npm run build` | exit 0 (`dist/validity-*.d.mts` incluido) |
| smoke | `npm pack --dry-run` | exit 0 — 153 archivos, 2.5 MB |
| cierre | `grep -rn "? 300 : 60" src/` | **0 coincidencias** |

## Cómo verificarlo una vez publicado

```bash
npm view uvd-x402-sdk version                      # 2.91.0
node -e "console.log(require('uvd-x402-sdk').DEFAULT_VALIDITY_SECONDS)"   # 300
```

Y la verificación que importa —la ventana que se firma de verdad— sobre una red
que no sea Base:

```bash
node -e "const {X402Client}=require('uvd-x402-sdk');(async()=>{const c=new X402Client({defaultChain:'avalanche'});await c.connectWithPrivateKey(process.env.KEY,'avalanche');const r=await c.createPayment({recipient:'0x000000000000000000000000000000000000dEaD',amount:'0.01'});const p=JSON.parse(Buffer.from(r.paymentHeader,'base64').toString());console.log(Number(p.payload.authorization.validBefore)-Math.floor(Date.now()/1000));})()"
# 300  (era 60 hasta 2.90.0)
```

La llave sale de una variable de entorno; no se escribe en ningún archivo ni se
imprime.

## Cómo se despliega

- **Publicar = empujar un tag `vX.Y.Z`.** `.github/workflows/publish.yml:5-7`
  dispara con `tags: ['v*']` (y `workflow_dispatch:` en `:4`). El job corre
  `npm ci` → `typecheck` → `test:run` → `lint` → `build` → `npm pack --dry-run`
  (`:28-43`) y recién entonces `npm publish --provenance --access public` (`:57`)
  con `NPM_TOKEN` (`:59`). Ese workflow está **active**.
- **El CI de PR no corre**: el workflow `CI` (`.github/workflows/ci.yml`) figura
  como `disabled_manually` en GitHub (`gh workflow list --all`), y hubo **0 runs
  en las últimas 24 h**. Este push no gasta presupuesto de Actions; por eso el
  pre-CI corrió entero en local.
- Runbook manual alternativo: `scripts/publish-npm.sh` (exige estar en `main`,
  árbol limpio y al día con `origin/main`).

## Revisión de seguridad (refutador aparte, antes del push)

Un agente `web3-security-auditor` (opus, solo lectura) atacó el diff local
**antes** del primer push. Veredicto: **CONDITIONAL**, con dos condiciones que
quedaron cumplidas en este mismo PR.

| # | sev. | hallazgo | qué se hizo |
|---|---|---|---|
| 1 | INFO | Subir 60→300 no abre vector nuevo: el nonce es de 32 bytes por firma (el corte es el mapping del token, no la ventana); un header interceptado solo puede mover `value` hacia `to`; el facilitador revalida el tiempo en el settle (`x402-rs` `src/chain/evm.rs:1977` → `assert_time`) y `max_timeout_seconds` no se compara nunca contra `valid_before` | nada, confirma el cambio |
| 2 | **MEDIUM** | `validitySeconds` sin techo, y `PaymentInfo` tiene forma de 402 (`src/types/index.ts:236`, `src/react/PaymentMethodPicker.tsx:79`): un vendedor podía pedir una ventana de un año. El camino `fetch()` está limpio —arma `PaymentInfo` campo por campo, `src/client/X402Client.ts:574-580`—; el riesgo es el integrador que pasa el 402 tal cual (hipótesis: no se verificó un consumidor real que lo haga) | **condición cumplida:** `MAX_VALIDITY_SECONDS = 3600` + `INVALID_CONFIG` por encima, con tests en los dos caminos |
| 3 | LOW | `1e30` pasaba el resolver y moría adentro de ethers como "overflow" envuelto en `PAYMENT_FAILED`, sin nombrar la ventana | cerrado por el techo; `1e30` está en el test |
| 4 | LOW | Otros `300` literales: adaptadores y `src/backend/index.ts:1520` | backlog |
| 5 | INFO | `validitySeconds` se ignora en silencio en redes no-EVM (`src/client/X402Client.ts:1204`) | backlog |
| 6 | INFO | `EVMProvider.signPayment` no ve la config del cliente, pero el cliente nunca rutea EVM por el adaptador (`:368-370`) | nada |
| 7 | **MEDIUM, pre-existente** | Nonce con `Math.random()` cuando no hay `window` (`src/client/X402Client.ts:1064-1069`, `src/providers/evm/index.ts:295-301`): en Node `window` no existe aunque `globalThis.crypto` sí, así que **todo firmante server-side usa un PRNG no criptográfico**. No permite forjar firmas; sí colisiones de nonce | **no tocado** (fuera de alcance); fila de backlog **P1** |
| 8 | LOW | Test de cliente mal configurado afirmaba solo `X402Error`; holgura de reloj de 1 s frágil en CI | **condición cumplida:** ahora `toMatchObject({ code: 'INVALID_CONFIG' })` y holgura de 3 s |

## Estado de la fila del encargo

**Fila [4] — «La autorización EIP-3009 vive 60 s en 11 de las 12 redes»:
CERRADA en lo que se hace desde este worktree, con dos criterios bloqueados por
el merge.**

| criterio de cierre del spec | estado |
|---|---|
| `grep -rn "? 300 : 60" src/` = 0 | ✅ 0 coincidencias |
| tests nuevos verdes | ✅ 11/11, y ROJO comprobado contra el código viejo |
| `npm view uvd-x402-sdk version` con la versión nueva | ⏳ **bloqueado**: publicar es empujar el tag `v2.91.0`, y la sección 0 del encargo dice que eso lo hace c0der después del merge |
| issue #2 cerrado citando el commit | ⏳ **bloqueado** por lo mismo: se cierra cuando el commit exista en `main` |

La evidencia del spec se verificó y era **correcta**: los dos sitios estaban tal
cual decía, y el issue #2 sigue OPEN (`gh issue view 2`). No refuté ninguna fila.

## Para c0der — lo que NO hice y por qué

1. **No publiqué ni cerré el issue #2.** El encargo dice «publicar = tag vX.Y.Z
   (lo hace c0der despues del merge)». Al mergear: `git tag v2.91.0 && git push
   origin v2.91.0` y cerrar el #2 citando el commit.
2. **No cableé `maxTimeoutSeconds` del 402 al firmante.** `parse402`
   (`src/client/X402Client.ts:625`) no lee ese campo y `X402PaymentOffer`
   (`src/types/index.ts:298`) no lo tiene, así que honrar la ventana que el
   vendedor declara exige extender el parseo — fuera del alcance de la sección 2.
   Hoy no hace falta: el default 300 **es** lo que declara el facilitador. Queda
   como fila de backlog.
3. **No toqué los adaptadores ni el SDK Python**, aunque escriben el 300 a mano
   (wagmi/ows/env-key) y 3600 (Python). Backlog.
4. **El techo de 3600 s NO estaba en el encargo; lo agregué por la revisión de
   seguridad** (hallazgo MEDIUM, abajo). Es el único agregado fuera de la letra de
   la sección 2 y lo dejo explícito por eso: sin techo, el parámetro nuevo que
   pedía el encargo abría un camino para que un vendedor hiciera firmar una
   autorización de un año. Si c0der prefiere otro número, es una constante
   (`src/utils/validity.ts`, `MAX_VALIDITY_SECONDS`) más el test que la ata.
5. **No hice `npm ci` con cambios en el lock**: el bump salió de
   `npm version 2.91.0 --no-git-tag-version`, que mueve `package.json:3` y
   `package-lock.json:3,9`.

## Filas de backlog nuevas

| Fecha | Item | Contexto | Prioridad |
|---|---|---|---|
| 2026-09-13 | El comprador ignora el `maxTimeoutSeconds` que el vendedor declara en su 402 | `parse402` (`src/client/X402Client.ts:625`) no lo lee y `X402PaymentOffer` (`src/types/index.ts:298`) no lo tiene. Con el default 300 alcanza hoy, pero un vendedor que necesite más no tiene cómo pedirlo sin que el comprador lo configure a mano | P2 |
| 2026-09-13 | **Nonce EIP-3009 con `Math.random()` en todo firmante server-side** | `src/client/X402Client.ts:1064-1069` y `src/providers/evm/index.ts:295-301` caen a `Math.random()` cuando `typeof window === 'undefined'`, que es siempre en Node aunque `globalThis.crypto.getRandomValues` exista. No forja firmas; sí da nonces predecibles y posibles colisiones (`authorization is used`). Hallazgo 7 del refutador, pre-existente. Arreglo probable: `globalThis.crypto` primero, y fallar ruidoso si no hay CSPRNG | **P1** |
| 2026-09-13 | Cuatro sitios escriben `300` a mano en vez de leer `DEFAULT_VALIDITY_SECONDS` | `src/adapters/wagmi.ts:142`, `src/adapters/ows.ts:243`, `src/adapters/env-key.ts:161` y, del lado vendedor, `src/backend/index.ts:1520`. Cambio sin comportamiento, cierra la regla de configuración centralizada | P3 |
| 2026-09-13 | `validitySeconds` se ignora en silencio en redes no-EVM | `src/client/X402Client.ts:1204` (camino del adaptador). Documentar que es solo EVM o rechazarlo con un error que lo diga | P3 |
| 2026-09-13 | TS y Python siguen difiriendo en el default de la ventana (300 vs 3600) | `uvd-x402-sdk-python` `src/uvd_x402_sdk/client.py:1839`. Ya no es 60x sino 12x, y ambos son configurables. Decidir: alinear a 300 o documentar la diferencia en el contrato cross-language (el job `cross-language` de `ci.yml:43` no la cubre) | P3 |
| 2026-09-13 | PR #22 abierto, `CONFLICTING`, 90+ archivos | Rama `fricciones-al-montar-un-cobro`; el doc que traía ya está en `main` vía #21. Candidato a cerrar sin mergear | P3 |
| 2026-09-13 | Fricción 11, puntos (1) y (3), siguen abiertos | `docs/fricciones-al-montar-un-cobro.md:166-186`: un `onVerdict`/log con `invalidReason` en `FacilitatorClient`, y un mapa `invalidReason → texto para la persona`. El punto (2) lo cierra esta versión | P2 |

LISTO PARA c0der — rama `0xultravioleta/ts-ventana-3009`, un solo push; el SHA va en el `worker_done`. Falta: refutador de c0der si lo quiere aparte del de este handoff, merge, `git tag v2.91.0 && git push origin v2.91.0`, y cerrar el issue #2 citando el commit.
